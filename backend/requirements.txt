fastapi==0.111.0
uvicorn==0.29.0
python-dotenv==1.0.1
pydantic==2.7.1
numpy==1.26.4
openpyxl==3.1.2          # xlsx import
python-multipart==0.0.9  # file upload
PyJWT==2.8.0             # auth tokens
pyodbc==5.1.0            # Azure SQL (prod; local dev uses SQLite)
openai==1.30.5           # Azure OpenAI (optional; deterministic fallback otherwise)
pytest==8.2.0            # dev only
# Optional, heavy build (numpy fallback is used otherwise):
# prophet==1.1.5
